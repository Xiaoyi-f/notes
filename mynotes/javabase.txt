java下载配置环境变量 -> %JAVA_HOME%
maven下载配置环境变量 -> %MAVEN_HOME% -> conf/settings.xml配置
    1.配置镜像源
        <mirror>
          <id>idName</id>
          <mirrorOf>*</mirrorOf>
          <name>mirrorName</name>
          <url>http://my.repository.com/repo/path</url>
        </mirror>
    2.配置JDK版本(编译版本)
        <profiles>
            <profile>
                <id>jdk-config</id>
                <activation>
                    <activeByDefault>true</activeByDefault>
                </activation>
                <properties>
                    <maven.compiler.source>版本号</maven.compiler.source>
                    <maven.compiler.target>版本号</maven.compiler.target>
                    <maven.compiler.compilerVersion>版本号</maven.compiler.compilerVersion>
                </properties>
            </profile>
        </profiles>
    3.不同版本JDK配置不同对应源
           <profile>
             <id>jdk-x.x</id>

             <activation>
               <jdk>x.x</jdk>
             </activation>

             <repositories>
               <repository>
                 <id>jdkxx</id>
                 <name>特定JDK版本的特定使用源</name>
                 <url>对应源URL</url>
                 <layout>default</layout>
                 <snapshotPolicy>always</snapshotPolicy>
               </repository>
             </repositories>
           </profile>
    4.配置本地仓库路径 -> maven下载的依赖存放在本地的位置
        <localRepository>localPath</localRepository>
    5.mvn help:effective-settings 检查配置是否生效
maven项目结构:
    my-maven-project/                    # 项目根目录
    │
    ├── pom.xml                          # Maven核心配置文件
    │
    ├── src/                             # 源代码目录
    │   │
    │   ├── main/                        # 主代码目录（最终要打包的）
    │   │   ├── java/                    # Java源代码
    │   │   │   └── com/
    │   │   │       └── example/
    │   │   │           └── App.java     # 主类
    │   │   │
    │   │   ├── resources/               # 资源文件（配置文件等）
    │   │       ├── application.properties
    │   │       ├── logback.xml
    │   │       └── messages.properties
    │   │
    │   │
    │   │
    │   │
    │   │
    │   │
    │   │
    │   │
    │   └── test/                        # 测试代码目录（不会被打包）
    │       ├── java/                    # 单元测试代码
    │       │   └── com/
    │       │       └── example/
    │       │           └── AppTest.java # JUnit测试类
    │       │
    │       └── resources/               # 测试专用资源文件
    │           └── test-config.properties
    │
    ├── target/                          # 编译输出目录
    │   ├── classes/                     # 编译后的.class文件
    │   ├── test-classes/                # 测试编译后的.class文件
    │   ├── my-app-1.0.0.jar            # 打包后的jar文件
    │   ├── maven-status/                # Maven状态信息
    │   └── surefire-reports/            # 测试报告
    │
    ├── .gitignore                       # Git版本控制忽略文件
    ├── README.md                        # 项目说明文档
    └── LICENSE                          # 开源许可证
maven基本使用:
    mvn -version

    三大生命周期:
        # Clean生命周期 - 清理
        mvn clean          # 删除target目录

        # Default生命周期 - 构建（最重要）
        mvn compile        # 编译
        mvn test          # 测试
        mvn package       # 打包成jar/war
        mvn install       # 安装到本地仓库（其他项目可以用）
        mvn deploy        # 部署到远程仓库（公司内部共享）

        # Site生命周期 - 生成报告
        mvn site          # 生成项目文档网站


内置模块:
    Random:
        import java.util.Random;
        ...
        Random randomObj = new Random();
        // float 精度 6-7位    double 精度 15-17位
        // 绝大多数直接使用double是最万能的
        int intNum = randomObj.nextInt(10) + 10;
        float floatNum = randomObj.nextFloat()
        double doubleNum = randomObj.nextDouble();
        // Math.random() 等价类似于 randomObj.nextDouble();
        boolean randomBoolean = randomObj.nextBoolean();
        ...
    内置字符(串)相关方法/函数:
        .matches() 全量匹配，返回boolean
        .toCharArray()
        .charAt(int idx)
        .split(String regex)
        .subString(int startIdx, int endIdx)
        .replaceFirst(regexPattern, replaceContent)
        .replaceAll(regexPattern, replaceContent)
        .length()
        String.format("%%%c%s%d%m.nf%b%n")
        Character.isLowerCase(char x)
        Character.isUpperCase(char x)
        Character.isLetter(char x)
        Character.isWhitespace(char x)
        Character.toLowerCase(char x)
        Character.toUpperCase(char x)
        Tip: char x - '0' 可以实现字符显现数字
    所有类都继承自 Object 类:
        .equals()
        .toString()
        .class
    Scanner类:
        Scanner sc = new Scanner(System.in);
        除了 System.in 还可以 变量 或 文件 为输入源，将内容数据存到缓冲区使用
        ...
        sc.next() 读取一个词元，遇到空格或换行停止
        sc.nextLine() 读取一整行，直到遇到换行符
        sc.hasNextInt() 优先使用来检测避免报错
        sc.hasNextDouble() 优先检测避免报错
        sc.nextInt() 读取一个整数
        sc.nextDouble() 读取一个浮点数 double
        MatchResult scRes = sc.match();  --》 Matcher方法大多类似
        sc.close() 关闭资源
        ...
    Pattern类:
        ...
        Pattern pattern = Pattern.compile("regexExpr", mode); 预编译
        Matcher matcher = pattern.matcher(matchObj); 获取匹配器
            matcher.matches() 全量匹配
            matcher.find() 局部匹配
            matcher.group() 0 1 ...
            matcher.start() 包含
            matcher.end() 不包含
            matcher.reset(str)
    StringBuilder类:
        可变字符串构造，非线程安全的，需要加锁
        ...
        字符数量 1024个
        StringBuilder sb = new StringBuilder(1024);
        for (int i = 0; i < 1000; i++) {
            sb.append("数据").append(i).append(",");
        }
        sb.setLength(0) 清空
        sb.insert(index, content)
        sb.deleteCharAt(index)
        ...
    Formatter类:
        ...
        Formatter fmtStr = new Formatter();
        fmtStr.format("formatStr")
        String res = fmtStr.toString()
        Formatter fmtOut = new Formatter(System.out);
        fmtOut.format("直接打印%n");
        Formatter fmtFile = new Formatter(new File("log.txt"));
        fmtFile.format("写入文件内容");
    Arrays类:
        下面示例使用大小写不敏感比较器
        Arrays.sort(strArray, String.CASE_INSENSITIVE_ORDER);
        默认从小到大排序或者字母顺序排序，以下让包装类数组实现从大到小排列
        Arrays.sort(array, Collections.reverseOrder());
        int index = Arrays.binarySearch(array, target, String.CASE_INSENSITIVE_ORDER);
        Arrays.copyOf(array, lengthNum)
        Arrays.fill(array, val)
        Arrays.copyOfRange(array, startIdx, endIdx)
        Arrays.setAll(array, func) 各元素操作
        Arrays.parallelSetAll() 并行操作
        普通的 Arrays.toString() 只会显示内部数组的引用地址
        Arrays.deepToString() 会递归地转换所有层级的数组元素为字符串
        Arrays.stream()
            .skip(n)
            .limit(n)
            .map(func)
            .filter(func)
            .distinct() 去重
            .forEach(func)
        .length
    LocalTime类 和 LocalDate类:
        LocalTime localTime = new LocalTime();
        LocalDate localDate = new LocalDate();
    内置注解:
        @Override 标记以下方法/函数是重写
        @Deprecated 标记过时，之后不在使用
        @SuppressWarnings("抑制警告类型") 标记屏蔽报警
    Math类:
        Math.max(a, b)
        Math.min(a, b)
        Math.sqrt(num)
        Math.abs(num)
        Math.random()
        Math.pow()
        Math.round()
    集合相关:
        Collection 单列集合 一个接口:
        Collections.addAll(list, "val1", "val2" ... )
        实例.stream()
        boolean -> 实例.add(element)
        boolean -> 实例.contains(element) 底层依赖equils方法进行判断存在与否，如果是自定义对象为元素，一般重写equils方法
        void -> 实例.clear()
        boolean -> 实例.remove(element)
        boolean -> 实例.isEmpty()
        int -> 实例.size()
            |_ List 添加的元素是有序、可重复、有索引的 实例.set(idx, element) 实例.get(idx) 实例.remove(idx)
                |_ ArrayList(数组结构，查找快) LinkedList(双向链表，增删快) Vector
            |_ Set 添加的元素是不重复、无索引的
                |_ HashSet(无序 哈希表+链表+红黑树) TreeSet(自动排序 默认自然序或字典序)
                    |_ LinkedHashSet(有序，默认依插入顺序 哈希表+双向链表+红黑树)
        Map 双列集合(键值对，键唯一，值随意):
        实例.put(key, val) 添加/修改 若是修改则返回最终值，若是添加则返回null
        实例.remove(key) 返回被删除的键值对的值，若不存在对应key则返回null以示删除失败
        void 实例.clear()
        boolean 实例.containsKey(key)
        boolean 实例.containsValue(val)
        boolean 实例.isEmpty()
        实例.keySet()
        实例.get(key)
        实例.forEach((xx key, xx value) -> { System.out.println(key + "=" + value) })
        实例.entrySet() 返回包含entry实例的集合
        entry实例.getKey()
        entry实例.getValue()
        int 实例.size()
            |_ HashMap 哈希表+链表+红黑树 无序 允许key/value为null
            |_ LinkedHashMap 哈希表+双向链表 有序(插入/访问顺序) 允许key/value为null
            |_ TreeMap 红黑树 自然顺序 key/value不可以为null
        不可变集合: List.of(), Set.of(), Map.of() 不能改动只能单纯使用
            不可变集合不能包含null
            元素本身是可变对象，对象内部数据可以改
    迭代器Iterator类与其他 集合专用的遍历方式:
        Iterator<E> 实例 = 集合实例.iterator() 返回迭代器对象，默认指向当前集合的0索引
        boolean 实例.hasNext() 判断当前位置是否有元素，有元素返回true，没有返回false
        E 实例.next() 获取当前位置元素，并将迭代器对象移向下一个位置
        迭代器遍历完不会复位指针，如果结束仍调用会报错 NoSuchElementException
        迭代器遍历时不能够使用集合的方法进行增加或删除，要想在遍历的同时进行集合的增删操作需要使用迭代器方法
        --> void 迭代器实例.remove() 删除当前元素
        以下 --> 指向内容只有List集合可以使用
        --> void 迭代器实例.set() 设置当前元素值
        --> void 迭代器实例.add() 在当前元素前一位置添加元素
        --> boolean 迭代器实例.hasPrevious() 检测是否有前面一个元素
        --> E 迭代器实例.previous() 获取前面一个元素
        for ( : ) 底层由迭代器实现
        集合实例.forEach(lambda表达式(箭头函数)) 箭头函数方法体只有一行则可以将大括号和return省略
    Java特性之方法引用:
        静态方法引用: 类名::静态方法名
        实例方法引用  对象::实例方法名 类名::实例方法名
        构造方法引用  类名::new
    Java异常分类:
        有些数据是在运行时才能具体，然后有些异常是因这些而起
        编译时异常(javac命令): 编译时候就会报错，直接继承Exception类，大多本地数据引起
        运行时异常(java命令): 运行时才报错，继承RuntimeException
        try catch finally
        throws 写在方法后，告诉调用者可能出现的异常
        throw 写在方法内，手动抛出异常对象
    File类和FileWriter类 -> File指代文件/文件夹:
        import java.io.File;
        File file = new File("filename.txt");
        基本方法:
            boolean -> canRead()
            boolean -> canWrite()
            boolean -> createNewFile()
            boolean -> delete()
            boolean -> exists()
            String -> getName()
            String -> getAbsolutePath()
            long -> length()
            String[] -> list()
            boolean -> mkdir()
        import java.io.FileWriter;
        # Windows 中文版 → GBK / ...
        # Linux / Mac → 通常是 UTF-8
        import java.nio.charset.StandardCharsets;
        FileWriter writer = new FileWriter("filename.txt", StandardCharsets.UTF_8);
        writer.write("文本内容");
        writer.println("自动换行文本句");
        writer.close();
        # 使用Scanner读取文件内容
        # System.lineSeparator() 系统换行符
        File file = new File("filename.txt");
        Scanner reader = new Scanner(file);
        while (reader.hasNextLine()) {
            String data = reader.nextLine();
            System.out.println(data);
        }
        reader.close();
        # 自动关闭
        try (FileWriter writer = new FileWriter("test.txt", StandardCharsets.UTF_8)) {
            writer.write("内容");
        } // 自动 close
    IO流:
        字节流(InputStream / OutputStream)和字符流(Reader / Writer): 字节流可以操作各种文件，但是字节流只能操作文本文件
        输入流（Input）：从文件/网络 → 程序（读）
        输出流（Output）：从程序 → 文件/网络（写）
        // true 表示开启 追加模式 
        FileInputStream fis = new FileInputStream("path/filename", true);
        int b = fis.read(); // 读取一个移动一下
        System.out.println((char)b);
        fis.close();
        读取到末尾返回-1 
        void write(int b) 一次写一个字节数据
        void write(byte[] b) 一次写一个字节数组数据
        void write(byte[] b, int startindex, int len) 一次写一个字节数组的部分数据
        FileOutputStream fos = new FileOutputStream("path/filename", true);
        String str = "str";
        byte[] bytes = str.getBytes();
        fos.write(butes);
        // \r\n 为win的换行 \n 为linux/mac的换行
        fos.close();
        输入流输出流先后创建对象要规范，最后先创建对象的最后关闭
    并发:
        多线程与多进程:
        pass
    网络编程:
        pass
    反射:
        反射允许对成员变量(字段)、成员方法和构造方法的信息进行编程访问
        修饰符、异常、名字、类型、值、形参、创建对象、返回值、注解、运行方法

        # 获取class对象的三种方式:
        1.Class.forName("全类名");
        2.类名.class
        3.对象.getClass();

        # class类中用于获取构造方法的方法
        Constructor<?>[] getConstructors() 返回所有公共构造方法对象的数组
        Constructor<?>[] getDeclaredConstructors() 返回所有构造方法对象的数组
        Constructor<T> getConstructor(Class<?>...parameterTypes) 返回单个公共构造方法对象
        Constructor<T> getDeclaredConstructor(Class<?>...parameterTypes) 返回单个构造方法对象

        # demo
        // 获取class字节码文件对象
        Class classObj = Class.forName("全类名");
        // 获取构造器对象
        Constructor[] cons = classObj.getConstructors();
        Constructor con = classObj.getDeclaredConstructor(String.class);
        int modifiers = con.getModifiers();  // 对应的数字对应的修饰符 从API文档查reflect找
        Parameter[] parameters = con.getParameters();
        # 临时取消权限校验
        con.setAccessible(true);
        Student stu = (Student)con.newInstance("张三", 18);
        System.out.println(stu);
        // 获取成员变量
        Field[] fields = classObj.getDeclaredFields();
        Field name = classObj.getDeclaredField("name");
        int modifiers = name.getModifiers();
        Class<?> type = name.getType();
        Student stu = new Student("张三", 18);
        name.setAccessible(true);
        String value = (String)name.get(stu);
        System.out.println(value);
        name.set(stu, "李四");
        System.out.println(stu);
        // 获取成员方法
        Class classObj = Class.forName("全类名");
        // 获取所有方法，包含父类
        Method[] methods = classObj.getMethods();
        for (Method method : methods) {
            System.out.println(method);
        }
        // 获取所有方法，不含父类，但是可以获取本类中私有的
        Method method = classObj.getDeclaredMethod("eat", String.class);
        int modifiers = method.getModifiers();
        String name = method.getName();
        Parameter[] parameters = method.getParameters();
        Class[] exceptionTypes = method.getExceptionTypes();
        Student stu = new Student();
        method.setAccessible(true);
        String result = (String)method.invoke(stu, "汉堡包");
        System.out.println(result);

    代理:
        代理可以无侵入式的给对象增强其他的功能
        调用者 -> 代理 -> 对象
        代理中就是对象需要被代理的方法
        java中通过接口保证代理样子

        静态代理:
            静态代理（最简单，但笨重）


            // 1. 接口（保证代理样子）
            public interface BankService {
                void transfer(String from, String to, int money);
            }

            // 2. 原始对象（被代理的）
            public class BankServiceImpl implements BankService {
                @Override
                public void transfer(String from, String to, int money) {
                    System.out.println("转账 " + money + " 元");
                }
            }

            // 3. 代理类（手写，和原始类实现同一接口）
            public class BankServiceProxy implements BankService {
                private BankService target;  // 持有原始对象
                
                public BankServiceProxy(BankService target) {
                    this.target = target;
                }
                
                @Override
                public void transfer(String from, String to, int money) {
                    // 前置增强
                    System.out.println("开始日志...");
                    long start = System.currentTimeMillis();
                    
                    // 调用原始对象
                    target.transfer(from, to, money);
                    
                    // 后置增强
                    System.out.println("耗时: " + (System.currentTimeMillis() - start) + "ms");
                }
            }

            // 4. 使用
            BankService service = new BankServiceProxy(new BankServiceImpl());
            service.transfer("A", "B", 100);

            缺点：每个被代理的类都要手写一个代理类 → 代码冗余

        动态代理:
        public class JdkProxyDemo {
        public static void main(String[] args) {
            // 1. 原始对象
            BankService target = new BankServiceImpl();
            
            // 2. 创建代理
            BankService proxy = (BankService) Proxy.newProxyInstance(
                target.getClass().getClassLoader(),  // 类加载器
                target.getClass().getInterfaces(),   // 要代理的接口
                new InvocationHandler() {             // 增强逻辑
                    @Override
                    public Object invoke(Object proxy, Method method, Object[] args) throws Throwable {
                        // 前置增强
                        System.out.println("【JDK动态代理】方法: " + method.getName() + " 开始执行");
                        long start = System.currentTimeMillis();
                        
                        // 调用原始方法
                        Object result = method.invoke(target, args);
                        
                        // 后置增强
                        System.out.println("【JDK动态代理】耗时: " + (System.currentTimeMillis() - start) + "ms");
                        return result;
                    }
                }
            );
            
            // 3. 调用代理
            proxy.transfer("A", "B", 100);
        }
    }

    执行流程：
    调用 proxy.transfer()
    → InvocationHandler.invoke() 
        → 前置增强
        → method.invoke(target, args)  // 真正调用原始对象
        → 后置增强

    关键点：
    - Proxy.newProxyInstance 在内存中动态创建了一个类
    - 这个类实现了你指定的接口
    - 所有方法调用都会转发到 InvocationHandler.invoke()


1.万物皆对象
2.一段程序实际上就是多个对象之间通过发送消息来通知彼此要干什么
3.从内存角度而言，每一个对象都是由其他更为基础的对象组成的
4.每一个对象都是类型
5.同一类型的对象可以接收相同的消息
6.对象具有状态、行为和标识
7.对象具有接口，接口约束类型
8.对象可以提供服务
9.程序具有高内聚和低耦合的话，对于项目非常有利
10.类的关键或者薄弱的地方需要进行隐藏、封装
11.学会复用代码和组合代码
12.java类只支持单继承
13.多态即同一个行为（方法）作用于不同的对象时，可以表现出不同的实现结果
14.Java中所有类都直接或间接继承自同一个根类 Object，像一棵树从同一个根长出所有分支，我们把这个称为单根层次结构
15.数组只能存放一种数据类型数据，集合可以存放不同的数据类型数据
16.JVM是java的运行虚拟机
17.异常处理可以实现异常的捕获以即后继操作
18.明确声明和初始化的区别
19.基本的存储形式: 寄存器 栈 堆 常量存储 非RAM存储
20.只是声明的话，一般底层会进行初始化值为null，使得程序更加安全
21.java注释 /* 内容 */    // 内容
22.作用域会决定变量的可见性和生命周期
23.java中对象的生命周期和基本数据类型的不同
    new创建的对象，即使是离开对应作用域，对应的标识符不再能使用，但是底层的内存还是没有清空，然而new创建的对象会被GC监控消除
24.字段分为实例字段和类字段，对应python的实例变量和类变量，具体的使用范围可以使用关键词进行限定
访问控制修饰符:
    public 公共的，可以被任何其他类访问
    protected 受保护的，对于同一包内所有类和无论何处包的任何子类可见
    default 默认全选，只对于同一个包内类可见
    private 私有，只能在定义的类中可见，其他任何地方不可见
非访问控制修饰符:
    static 类和实例均可直接调用
    final 使不可变
25.java确保命名空间不冲突的方式: 将域名反转逐层命名
26.运算符
    括点：() [] .
    单目：++ -- ! ~
    乘除：* / % + -
    位：<< >> & ^ | [>>>(基本用不到)]
    关系：< > <= >= == != instanceof
    逻辑：&& || 短路特性
    三目：? :
    赋值：= 以即 复合赋值(+=、-=...) 以即 右结合连续赋值
    注意: == 对于基本数据类型比较的是数值，对于引用数据类型比较的是地址，Integer要注意超出对应的缓存范围就会新开地址
    提示: java有个特性 -> 方法引用 前::后 即 前接收数据 再执行 后操作
27.基本数据类型
    byte(1)、char(2)、short(2)、int(4)、long(8)、float(4)、double(8)、boolean看JVM
    对应的包装类: Byte Character Short Integer Long Float Double Boolean
    Integer 默认缓存 -128 到 127
    new 方式会无视缓存直接创建新的内存和地址，但是.valueOf会优先使用缓存，缓存池生效
    Java 的集合框架（如 ArrayList, HashMap）和泛型只能存储对象，不能存储基本类型。包装类是解决这个问题的唯一途径
    包装类提供了大量的工具函数
    自动装箱和自动拆箱:
        Integer i = 100; // 等价于 Integer i = Integer.valueOf(100);
        int n = i; // 等价于 int n = i.intValue();
    基本数据类型内存不可变
28.字面量
    后缀: L、F、D
    前缀: 0B 0 0X
    数字字面量允许使用下划线分割
    科学计数法中间符: E
29.标签: 以自定义标识符和冒号实现管理代码块，经常配合 continue 标识符; 和 break 标识符; 使用
30.java可通过构造器(方法)保证初始化和GC进行垃圾回收管理
31.java支持方法重载以即嵌套类
32.java中通过this关键词标识实例引用
33.java --list-modules 列出所有可使用模块
    @标识正在使用的jdk版本
34."委托"是一种设计思想——把任务交给另一个对象去执行
35.把子类对象当作父类类型来用即向上转型，向上转型只能够使用子类和父类共同拥有的方法或者属性，但是实际使用的是子类的方法或者属性
36.Python不需要显式构建，是因为它的“构建”步骤被解释器和包管理工具隐藏了；而Java需要构建，是因为从源代码到可运行程序之间，多了一个编译和打包的环节
37.用户请求服务都是会带有对应的ip的，服务的后端可以实现检测某些情况进行ip封锁
38.如果使用+的拼接功能，若周围是字符串则会进行隐式字符串转换，且拼接运算总是在单目运算完全之后
39.基本输出方式System.out.println() System.out.printf()
    自动换行输出只能使用+拼接
    格式化输出可以使用如下占位符:
        %d %s %f(无论float或double) %c %b(boolean) %n(跨平台换行，\n是Unicode编码) %X %O %E %% （m.n）
40.java字符串编码默认使用UTF-16，java17以即之前版本默认编码依赖操作系统，之后版本默认UTF-8
41.基本条件控制:
    for for ( : )
    while
    if else if else
    switch () case : break; default:
42.java中除了构造方法、static方法、final方法、private方法，其他实例方法默认都是后期绑定的
43.private方法默认就是final特性的
44.具有final特性的方法不能在子类中进行重写，但是重载是没有问题的
45.静态字段在内存中只有一份，被整个应用程序的所有线程共享，静态方法在类加载时就存在，此时可能还没有创建任何对象，字段通常是private的
46.为了确保子类所有数据完善，基类的构造器会在子类的构造过程中被调用，若没有在子类中显示调用父类构造器，那么编译器会自动调用，若没有写构造器则编译器自动合成无参构造器
47.构造器名字需要和类名字一样，支持重载并且无返回类型，this()用于调用本类的其他构造器，super()用于调用父类的构造器
48.为了避免循环引用问题，大多java编译器的GC并没有使用引用计数功能，而是使用更加强大的可达性分析算法
49.Java的数组一定会被初始化，若没有手动初始化默认会给null，并且无法访问数组边界之外的元素
50.窄化转型就是说，将能容纳更多信息的数据类型转换成无法容纳那么多信息的数据类型
51.var会自动推断类型，推荐在局部变量中使用
52.final类禁止继承，它的所有方法都是隐式final的，因为无法重写它们
53.向上转型：子类对象转为父类引用，自动发生，绝对安全；向下转型：父类引用转回子类引用，必须强制进行，且存在风险
54.接口可以实现多个，类一般只能继承一个，可以通过继承扩展接口
55.sealed只允许指定的几个孩子，类前面加 sealed，并写明允许哪几个子类继承，示例: sealed class Animal permits Dog, Cat {}
56.non-sealed —— 重新“打开”继承，当你在 sealed 的子类上写 non-sealed，意思是：从这个子类往下，又可以随意继承
    示例:
        sealed class Animal permits Dog {}
        non-sealed class Dog extends Animal {}
        class AnyDog extends Dog {}   // ✅ 允许
57.接口中的任何类都会自动成为public和static的。因为类是static的，所以被嵌套的类只是放在了这个接口的命名空间内
58.静态导入
    只能 import static 那些用 static 修饰的方法或变量，如果你试图导入一个普通方法（非静态方法），编译器会直接报错
    不要滥用通配符 *：
        虽然 import static onjava.Range.*; 很方便，但如果两个不同的类里都有同名的静态方法（比如 Math.max 和 MyUtils.max），你同时用 * 导入它们，编译器就会“懵圈”（报错：引用不明确），因为它不知道该用哪一个
59.System.exit(0) 执行安全退出程序
60.基本类型数组（如 int[]）初始化后会有默认值（0），而包装类数组（如 Integer[]）初始化后全是空（null）
61.java的箭头函数可以使用this
62.只有实现Iterable接口的对象才有资格调用.forEach()
63.java直接支持抽象类 abstract 和 接口 interface 以即 枚举 enum enum有.values() 可以获得枚举常量值数组
64.java支持变长参数 类型... 参数名 实际本质就是一个数组
65.面向对象除了理解基本的封装继承和多态之外还有一个组合是必须知道的
66.java支持泛型,声明泛型之后就可以直接使用类型参数代类型，最终实际使用传递真实类型就可以
    泛型类: 在类名后面加 <T>，T 就是类型参数 这个不一定是要 T，也不是只能有一个类型参数，实例化时候在类名后面依次传递对应的类型就行
    泛型接口: 定义接口时候在接口名后加 <T> 实现时候 在接口后面传递对应的类型就行，当然也可以不传递实际数据类型，依旧写明 T 标明依旧使用泛型，还未传递具体类型
    泛型方法: 在方法的返回类型前面加 <T>，表示这是一个泛型方法
    类型参数约定:
        T	Type（类型）	一般类型
        E	Element（元素）	集合中的元素，如 List<E>
        K	Key（键）	Map 的键，如 Map<K, V>
        V	Value（值）	Map 的值，如 Map<K, V>
        N	Number（数字）	数字类型
        R	Result（结果）	返回结果类型
    泛型通配符:
        无界通配符 <?> 表示任意类型，常用于只读场景
        上界通配符 <? extends T> 表示T 或 T 的子类，用于生产者场景（只读）
        下界通配符 <? super T> 表示T 或 T 的父类，用于消费者场景（写入）
    PECS(Producer Extends, Consumer Super):
        <? extends T>	生产者（提供数据）	✅ 可读为 T	❌ 不可写	生产（读）用 extends
        <? super T>	消费者（接收数据）	✅ 可读为 Object	✅ 可写 T 及子类	消费（写）用 super
        <?>	未知类型	✅ 可读为 Object	❌ 不可写（除 null）	任意类型
    Java 的泛型是伪泛型。泛型信息只在编译阶段存在，编译成字节码后，泛型信息会被擦除
    擦除规则:
        原始代码	擦除后（字节码）
        Box<T>	Box（T 擦除为 Object）
        Box<T extends Number>	Box（T 擦除为 Number）
        List<? extends Number>	List（擦除为 Number）
        List<? super Integer>	List（擦除为 Object）
67.java的箭头函数即java的lambda表达式，java的箭头函数的立即执行式和js的一个逻辑
68.stream不是数据结构，他是数据处理的流水线，集合自带.stream() 获得 stream 对象，有一个东西叫做并行流...



