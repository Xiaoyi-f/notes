min-width max-height line-height 
cursor -> pointer text not-allowed grab grabbing
vertical-align text-align text-decoration 
white-space -> nowrap normal wrap
word-break: break-all;
position top right bottom left z-index
relative absolute sticky(最近滚动容器) fixed 
display inline inline-block block 
display none -> DOM
visibility hidden visible
user-select pointer-events 
overflow-x overflow-y hidden 
text-overflow: ellipsis;
text-shadow box-shadow
offset-x offset-y blur-radius color

/* 设置蒙层 */
background-color
filter: blur() opacity() brightness()
mask-image: linear-gradient(direction, color1, color2);
mask-size: cover;

object-fit 
aspect-ratio 
background-color 
background-image
background-size: cover;
background-position 

color 
rgba
transparent

/* 文字渐变 */
background: linear-gradient(direction, color1, color2, ...);
background-clip: text;
color: transparent;

px em rem ch 
% vw vh auto 
minmax(min, max)
fr repeat(n, xfr) n --> auto-fill(留白) auto-fit(全取)

xxx-image ->
linear-gradient(direction, color1 positionpercent1, color2 positionpercent2, ...)
direction -> 正上为零，顺时针转

font-family 
font-size
font-weight
letter-spacing 
scrollbar-width 
scrollbar-color 滑块色 背景色
::-webkit-scrollbar-track
::-webkit-scrollbar-thumb

list-style 
:visited
:hover
:active

transition: attribute time delay;  all 
transform 可以配置同时多个值
transform -> translate(x, y) scale(n) rotate(xdeg)
animation: animationName time infinite;
animation-play-state: paused;
@keyframes animationName {
    0% {
        pass 
    }
    x% {
        pass
    }
}
@media (max-width: 1024px) and (min-width: 768px) {
    pass 
}

.el-message.xxx {
    pass 
}

display: flex;
flex-direction row column 
flex-wrap nowrap wrap wrap-reverse
justify-content align-item -> 对齐类: flex-start flex-end center 分部类: space-between space-around space-evenly
align-items -> stretch(默认) flex-start flex-end center 
flex: flex-basis flex-grow flex-shrink; 默认值: auto 0 1; --> 占自己份额,数字表示占比份数,100%表示占整个容器宽度
flex-shrink设置为0，但是给flex-grow设置为1，flex-basis即可分别表示对应的剩余占比
计算容器宽度 : flex-basis --> (> flex-grow) (= flex-basis) (< flex-shrink)
align-self -> flex-start flex-end center baseline stretch 
order 视觉排列顺序，DOM不改动

display: grid;
grid-template-areas:
    "header header header"
    "sidebar main aside"
    "footer footer footer";
grid-template-rows / grid-template-columns
gap x y 


