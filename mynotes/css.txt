行内 > 内联 > 外接 (由内向外)
!important > id > class : [] > element :: > * + > "守村人比外归人穷,id唯一身份高,物以稀为贵"
margin
padding
border
outline
box-sizing
none
border-box(border内向)

# 关闭 / 打开双指缩放
touch-action
manipulation / pinch-zoom

user-select
min-height
max-width
overflow-x
overflow-y
hidden
background-color
background-image
background-size cover
background-position center left right top bottom
color
font-family
font-size
font-weight
letter-spacing
scrollbar-width
scrollbar-color
::-webkit-scrollbar-track
::-webkit-scrollbar-thumb
object-fit
display block inline-block flex grid
visibility
text-decoration
lv好 ->
:visited
:hover
:active
list-style
position relative absolute sticky fixed
z-index top bottom left right
cursor pointer text not-allowed grab grabbing
pointer-events
filter blur
opacity
mask
mask-image
mask-position
mask-size
white-space nowrap normal wrap
word-break break-word break-all
text-overflow ellipsis
text-align left center right
box-shadow
text-shadow
transition
all
transform
translate
scale
animation animationName time infinite/具体次数
@keyframes animationName
@media

.el-message.no-icon-message .el-message__icon {
    display: none;
}

.el-message.xxx {
    box-shadow
    text-shadow
}

display flex grid none
flex/grid 中 子元素 不要使用 display: none;
visibility: hidden; 隐藏但是保留占位 visible

flex 适合一维布局:
    左 -> 右 上 -> 下
    flex-direction row(默认) column
    flex-wrap nowrap(默认) wrap wrap-reverse(新行出现在上方)
    主轴: justify-content(整体元素)  align-item(管理多行并且真实换行) 对齐类: flex-start flex-end center 分布类: space-between space-around space-evenly
    交叉轴: align-items(管理单行) stretch(默认,没给高度或为auto则被拉伸与容器同一高度) flex-start flex-end center
    flex: flex-basis flex-grow flex-shrink; 基准参考值 扩展因子 收缩因子
    比较计算容器宽度与flex-basis，容器宽度大则使用flex-grow，小则使用flex-shrink
    flex: auto 0 1; (默认值)
    align-self flex-start flex-end center baseline stretch
    order 排列顺序 数值越小排列越靠前 只改变视觉 不改变DOM -> 键盘Tab键焦点跳动问题、对于视觉障碍/使用屏幕阅读器的用户不友好
grid 适合二维布局 同时定义行和列:
    grid-template-areas:
        "header header header"
        "sidebar main aside"
        "footer footer footer";
    grid-template-rows // 决定网格线定义
    grid-template-columns // 决定网格线定义
    gap: x y; // 间隔
    # n * n 网格 有 (n + 1) * (n + 1) 条网格线
    grid-column: startNum / endNum;
    grid-row: startNum / endNum;
固定单位: px em rem
剩余空间计算单位: fr repeat(n, xfr) n可以为 auto-fill(留白) auto-fit(充分利用空间)
相对单位: % vw vh
自动单位: auto
计算函数: minmax(min, max)

linear-gradient(xdeg, color1 x%, color2 x%)
url(图片url)

常规平台像素宽度:
手机端 < 768px
平板端 768px < x < 1024px
电脑端 > 1024px
